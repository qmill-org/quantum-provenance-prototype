# Supplementary material

This directory contains supplementary material for the paper. None of it is
required to run the artifact.

## Contents

### 1. Generated prototype record

- [`complete-example-provenance-response.json`](complete-example-provenance-response.json) —
  a full, schema-valid `ProvenanceRecord` as returned by
  `GET /v1/jobs/{job_id}/provenance`, generated by the artifact from the Braket
  fixture (the most fully populated case). Every top-level section is present:
  `job`, `device`, `compilation`, `software`, `execution`, `characterization`,
  `results`, `evidence`, `artifacts`, `operations`, and `warnings`.

  This file is a **generated artifact**: it is produced by the prototype
  implementation from the sanitized Braket fixture and committed here as a
  canonical example of a schema-valid `0.1.0-prototype` record.

### 2. Conceptual API example

- [`conceptual-api/complete-example-provenance-response.json`](conceptual-api/complete-example-provenance-response.json) —
  an **illustrative conceptual API** example with schema version `1.0.0`.
  This file is explicitly not generated by the evaluated prototype and is not a
  verbatim provider response.
- [`conceptual-api/README.md`](conceptual-api/README.md) — scope notes for the
  conceptual example and its separation from generated prototype artifacts.

### 3. Detailed 15-platform profile

- [`provider-profiles/platform-specific-findings.md`](provider-profiles/platform-specific-findings.md) —
  detailed, evidence-safe platform-specific findings for all 15 analyzed
  platforms (12 hardware-provider platforms + 3 cloud aggregators), aligned to
  the paper's assessed interfaces and evidence boundary.

### 4. Fixture-oriented provider profiles and notes

- [`provider-profiles/`](provider-profiles) — structured JSON profiles for the
  prototype-supported fixtures (Amazon Braket, IBM Quantum, IonQ), mapping each
  fixture field to the corresponding concept in public provider schemas.
- [`provider-evidence/amazon-braket-fixture.md`](provider-evidence/amazon-braket-fixture.md),
  [`provider-evidence/ibm-quantum-fixture.md`](provider-evidence/ibm-quantum-fixture.md), and
  [`provider-evidence/ionq-fixture.md`](provider-evidence/ionq-fixture.md) —
  fixture-specific companion notes for the executable prototype context.

### 5. 15-platform evidence ledgers

- [`provider-evidence/`](provider-evidence) — one Markdown evidence ledger per
  analyzed platform (all 15 platforms: 12 hardware-provider platforms and 3
  cloud aggregators). Each ledger documents:
  - the platform classification and evidence boundary;
  - the verification date;
  - a table of public documentation, SDK, and API sources with stable URLs;
  - notes distinguishing documented interface structure from actual field
    population.

  See [`provider-evidence/README.md`](provider-evidence/README.md) for the
  complete platform index with direct links to all 15 ledgers.

## Synthetic fixtures ↔ public provider evidence

The fixtures under [`../tests/fixtures/`](../tests/fixtures) are **synthetic,
hand-authored reconstructions**, not captures of real jobs:

- Their **structure and field semantics** faithfully mirror each provider's
  public schemas (see the profiles and evidence notes here).
- All **secrets and account identifiers are placeholders** (`FAKE-*` markers,
  example ARNs / IDs). The redaction layer and its tests assert such material
  never reaches an emitted record.
- No **proprietary or exported provider data** is included, keeping the artifact
  freely redistributable and fully deterministic.

The evidence ledgers reference official provider documentation, SDK source
repositories, and package release pages. Exact field paths in the fixtures are
reconstructions from public documentation and the corresponding open-source
SDKs. No ledger claims authenticated operational verification.
